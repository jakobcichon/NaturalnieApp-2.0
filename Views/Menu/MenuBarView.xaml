<UserControl x:Class="NaturalnieApp2.Views.Menu.MenuBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NaturalnieApp2.Views.Menu"
             xmlns:vm="clr-namespace:NaturalnieApp2.ViewModels.Menu"
             xmlns:controls="clr-namespace:NaturalnieApp2.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="400" Background="#FFEADFCB">


    <UserControl.DataContext>
        <vm:MenuBarViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" MinHeight="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Logo image-->
        <Image Grid.Row="0">
            <Image.Source>
                <BitmapImage UriSource="/Images/Naturalnielogo.png"/>
            </Image.Source>
        </Image>

        <!--Buttons-->
        <ItemsControl Grid.Row="1"  ItemsSource="{Binding MenuBarViews}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <controls:MenuBarItem/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>

</UserControl>
