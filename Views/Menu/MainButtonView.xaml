<UserControl x:Class="NaturalnieApp2.Views.Menu.MainButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" FontSize="22" Padding="3" 
                Content="{Binding Name}" 
                Command="{Binding Command}" 
                CommandParameter="{Binding}" 
                Background="#FF2D9877" BorderBrush="#FF2D9877"/>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding SubButtonCollection}"
                      Visibility="{Binding Visibility}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button FontSize="16" Padding="3" Content="{Binding Path=DisplayText}"
                            Background="#FF5FA992" BorderBrush="Black"
                            Command="{Binding}" 
                            CommandParameter="{Binding}">
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


    </Grid>
</UserControl>
