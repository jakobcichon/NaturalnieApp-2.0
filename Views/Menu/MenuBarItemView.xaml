<UserControl x:Class="NaturalnieApp2.Views.Menu.MenuBarItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NaturalnieApp2.Views.Menu"
             xmlns:vm="clr-namespace:NaturalnieApp2.ViewModels.Menu"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400" Background="#FF65A992">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ItemsControl>
            <DataTemplate DataType="{x:Type vm:MenuBarItemViewModel}">
                <Button Content="{Binding MainMenuName}"/>
            </DataTemplate>
        </ItemsControl>

    </Grid>
</UserControl>
